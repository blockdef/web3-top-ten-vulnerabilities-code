title: Understanding Web3 Vulnerabilities
---

## Code Example

```javascript
contract IntegerOverflow {
    uint256 public balance = 100;

    function addToBalance(uint256 _addition) public {
        balance += _addition; // Vulnerable to integer overflow
    }
}
